import React, { FC } from 'react';
import { View, Text } from 'react-native';

const AnswerScreen: FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Answer Screen</Text>
    </View>
  );
};

export default AnswerScreen;
